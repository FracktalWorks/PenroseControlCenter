########################################
# BASE_PENROSE_SINGLE.cfg
# Common Hardware Configuration for Penrose Single Nozzle Printers
# Single X carriage, Dual Y motors, external stepper drivers
# Author: Vijay Raghav Varada
# Version: 1
########################################

########################################
# X Stepper - Motor M1 (Single Carriage)
########################################

[stepper_x]
step_pin: PE6
dir_pin: PE5
enable_pin: !PC14
microsteps: 8
rotation_distance: 20
full_steps_per_rotation: 200
step_pulse_duration: 0.000003  # 3us - safe margin over DM542T 2.5us minimum
endstop_pin: ^PF0
homing_speed: 60
homing_retract_dist: 10.0

########################################
# Y Stepper - Motor M4 (Primary Y)
########################################

[stepper_y]
step_pin: PB4
dir_pin: !PB3
enable_pin: !PB6
microsteps: 8
rotation_distance: 20
full_steps_per_rotation: 200
step_pulse_duration: 0.000003  # 3us - safe margin over DM542T 2.5us minimum
endstop_pin: ^PF2
homing_speed: 60
homing_retract_dist: 10.0

########################################
# Y1 Stepper - Motor M5 (Secondary Y)
########################################

[stepper_y1]
step_pin: PG13
dir_pin: !PG12
enable_pin: !PG15
microsteps: 8
rotation_distance: 20
full_steps_per_rotation: 200
step_pulse_duration: 0.000003  # 3us - safe margin over DM542T 2.5us minimum
endstop_pin: ^PF2

########################################
# Z Stepper - Motor M3
########################################

[stepper_z]
step_pin: PB8
dir_pin: !PB7
enable_pin: !PE0
microsteps: 16
rotation_distance: 4
full_steps_per_rotation: 200
step_pulse_duration: 0.000003  # 3us - safe margin over DM542T 2.5us minimum
endstop_pin: ^PF1
homing_speed: 10
second_homing_speed: 3
homing_retract_dist: 10

########################################
# Extruder 0 - Motor M6 (Single Toolhead)
# Penrose Pellet Auger Configuration
########################################

[extruder]
step_pin: PG9
dir_pin: PD7
enable_pin: !PG11
microsteps: 2
rotation_distance: 35.0  # Calibrated: 28.0 / 0.8 (was 80% flow in slicer)
step_pulse_duration: 0.000003  # 3us - safe margin over DM542T 2.5us minimum
nozzle_diameter: 0.4
filament_diameter: 1.75
heater_pin: PA1
max_power: 0.5  # Limit AC heater to 50% power
sensor_pin: PC5
sensor_type: ATC Semitec 104GT-2
control: pid
pid_Kp: 17.062
pid_Ki: 0.455
pid_Kd: 159.960
min_extrude_temp: 100
max_extrude_only_distance: 5000
max_extrude_cross_section: 8.0
min_temp: -200
max_temp: 300
# Pellet auger motion limits
max_extrude_only_velocity: 25
max_extrude_only_accel: 400
instantaneous_corner_velocity: 1.0
pressure_advance: 0
pressure_advance_smooth_time: 0.06

[verify_heater extruder]
max_error: 240
check_gain_time: 60
hysteresis: 40
heating_gain: 3

########################################
# Heater Bed
########################################

[heater_bed]
heater_pin: PA4
sensor_pin: PB1
sensor_type: ATC Semitec 104GT-2
control: watermark
min_temp: 0
max_temp: 150

########################################
# H0 - Secondary Heater for T0 (Pellet Barrel)
########################################

[heater_generic H0]
gcode_id: H0
heater_pin: PA0
max_power: 0.5  # Limit AC heater to 50% power
sensor_pin: PB0
sensor_type: ATC Semitec 104GT-2
control: pid
pid_Kp: 17.062
pid_Ki: 0.455
pid_Kd: 159.960
min_temp: -200
max_temp: 480

[verify_heater H0]
max_error: 240
check_gain_time: 60
hysteresis: 40
heating_gain: 3

########################################
# Probe and ABL
########################################

[probe]
pin: PD8
x_offset: 0
y_offset: 0
#z_offset: -0.55  # IMPORTANT: z_offset is managed by SAVE_CONFIG - do not set here!
sample_retract_dist: 2.0
speed: 4
lift_speed: 10
samples_tolerance: 0.05
samples: 1
samples_result: average
samples_tolerance_retries: 8

########################################
# Host Temperature Monitoring
########################################

[temperature_sensor CM4]
sensor_type: temperature_host
min_temp: 0
max_temp: 100

########################################
# Part Cooling Fans (2 fans for single toolhead)
########################################

[fan_generic extruder_CF_0]
pin: PF7

[fan_generic extruder_CF_1]
pin: PF9

########################################
# G-Code Arc Support
# Smoother curves = steadier flow for pellet extrusion
########################################

[gcode_arcs]
resolution: 0.5    # Balanced arc interpolation - reduces MCU load vs 0.1

########################################
# Misc.
########################################

[respond]
default_type: echo

[delayed_gcode STARTUP]
initial_duration: 1
gcode:
    RESPOND TYPE=echo MSG="Penrose Single Nozzle Ready"

########################################
# Tool Macros (Single Nozzle)
########################################

[gcode_macro PARK_TOOL]
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% if 'x' not in printer.toolhead.homed_axes %}
        RESPOND TYPE=echo MSG="PARK_TOOL skipped (X not homed)"
    {% else %}
        SET_GCODE_OFFSET X=0 Y=0 Z=0
        G90
        {% set XPOS = printer.configfile.config.stepper_x.position_min|float %}
        G1 X{XPOS} F{printer_var.feedrate}
    {% endif %}

[gcode_macro PURGE_POSITION]
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% if printer.toolhead.homed_axes != "xyz" %}
        RESPOND TYPE=echo MSG="PURGE_POSITION skipped (axes not homed)"
    {% else %}
        G90
        G1 X{printer_var.tool0_pause_position_x} Y{printer_var.tool0_pause_position_y} F9000
        RESPOND TYPE=echo MSG="T0 PURGE POSITION"
    {% endif %}

[gcode_macro T0]
description: T0 - Single nozzle (always active)
gcode:
    RESPOND TYPE=echo MSG="T0 active (single nozzle)"

########################################
# Temperature & Fans (Single Nozzle)
########################################

[gcode_macro M106]
description: Override "M106" for single toolhead with 2 fans.
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% set raw_speed = params.S|default(0)|float %}
    {% set fan_speed = (raw_speed / 255.0)|round(2) %}
    SET_FAN_SPEED FAN={printer_var.fan0_0} SPEED={fan_speed}
    SET_FAN_SPEED FAN={printer_var.fan0_1} SPEED={fan_speed}

[gcode_macro M107]
description: Override "M107" to turn off fans.
gcode:
    M106 S0

[gcode_macro M104]
rename_existing: M104.1
description: Override "M104" for single extruder.
gcode:
    {% set temp = params.S|default(0)|float %}
    {% set sec = params.H|default(-1)|int %}
    
    # Handle secondary heater (H0)
    {% if sec == 0 %}
        SET_HEATER_TEMPERATURE HEATER=H0 TARGET={temp}
    {% else %}
        M104.1 S{temp}
    {% endif %}

[gcode_macro M109]
rename_existing: M109.1
description: Override "M109" for single extruder.
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% set temp = params.S|default(0)|float %}
    {% set sec = params.H|default(-1)|int %}
    
    # Handle secondary heater (H0)
    {% if sec == 0 %}
        SET_HEATER_TEMPERATURE HEATER=H0 TARGET={temp}
        TEMPERATURE_WAIT SENSOR="heater_generic H0" MINIMUM={temp - 2}
    {% elif params.R is defined %}
        M109.1 S{params.R}
    {% else %}
        SET_HEATER_TEMPERATURE HEATER=extruder TARGET={temp}
        TEMPERATURE_WAIT SENSOR=extruder MINIMUM={temp - printer_var.offset_temp}
    {% endif %}

########################################
# Bed Mesh Calibration (Single Nozzle)
########################################

[gcode_macro G29]
gcode:
    BED_MESH_CLEAR
    CLEAR_PAUSE
    {% if printer.extruder.temperature|float <= 150 %}
        M109 S150
    {% endif %}
    BED_MESH_CALIBRATE
    BED_MESH_PROFILE SAVE=p1
    SAVE_CONFIG NO_RESTART=1
    M420 S1

########################################
# Homing Sequence (Single Nozzle)
########################################

[homing_override]
gcode:
    SET_GCODE_OFFSET X=0 Y=0 Z=0
    BED_MESH_CLEAR
    SAVE_VARIABLE VARIABLE=babystep_z VALUE=0.0
    SAVE_CONFIG NO_RESTART=1
    {% if params.X and not params.Y and not params.Z %}
        G28 X0
    {% endif %}
    {% if not params.X and params.Y and not params.Z %}
        G28 Y0
    {% endif %}
    {% if not params.X and not params.Y and params.Z %}
        G28 Z0
        G91
        G0 Z-4
        G90
    {% endif %}
    {% if params.Y and params.X and not params.Z %}
        G28 Y0
        G28 X
    {% endif %}
    {% if not params.X and not params.Y and not params.Z %}
        G28 Z0
        G28 Y0
        G28 X0
        G91
        G0 Z-4
        G90
    {% endif %}
    {% if params.X and params.Y and params.Z %}
        G28 Z0
        G28 Y0
        G28 X0
        G91
        G0 Z-4
        G90
    {% endif %}

########################################
# PAUSE & Resume (Single Nozzle)
########################################

[pause_resume]

[gcode_macro PAUSE]
rename_existing: BASE_PAUSE
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% set z = params.Z|default(2.5)|float %}
    {% set r = params.R|default(2.0)|float %}
    {% if printer['pause_resume'].is_paused|int == 0 %}
        SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={printer['extruder'].target|default(0)|int}

        SAVE_GCODE_STATE NAME=PAUSE
        BASE_PAUSE

        {% if printer.toolhead.homed_axes == "xyz" %}
            G91
            G1 E-{r} F600
            G90

            {% if (printer.gcode_move.position.z + z) < printer.toolhead.axis_maximum.z %}
                G91
                G1 Z{z} F900
                G90
            {% else %}
                RESPOND TYPE=echo MSG="Pause zhop exceeds maximum Z height."
            {% endif %}
        {% else %}
            RESPOND TYPE=echo MSG="Pause movement skipped (axes not homed)"
            G91
            G1 E-{r} F600
            G90
        {% endif %}

        PURGE_POSITION
    {% endif %}

[gcode_macro CLEAN_NOZZLE]
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% if printer.toolhead.homed_axes == "xyz" %}
        G90
        G1 X{printer_var.tool0_pause_position_x} Y{printer_var.tool0_pause_position_y} F7000
        G1 X{printer_var.tool0_pause_position_x + 40} F7000
        G1 X{printer_var.tool0_pause_position_x}
        G1 X{printer_var.tool0_pause_position_x + 40}
        G1 X{printer_var.tool0_pause_position_x}
    {% else %}
        RESPOND TYPE=echo MSG="CLEAN_NOZZLE skipped (axes not homed)"
    {% endif %}

[gcode_macro RESUME]
rename_existing: BASE_RESUME
variable_etemp: 0
gcode:
    {% set printer_var = printer["gcode_macro PRINTER_VARIABLES"] %}
    {% set e = params.E|default(20)|float %}
    {% set r = params.R|default(1)|float %}
    {% if printer['pause_resume'].is_paused|int != 1 %}
        RESPOND TYPE=echo MSG="RESUME ignored (not paused)"
    {% else %}
        # Restore temps
        {% if etemp > 0 %}
            M104 S{etemp|int}
            RESPOND TYPE=echo MSG="Heating extruder .."
            M109 S{etemp|int}
        {% endif %}

        PURGE_POSITION

        # Determine if we can safely prime
        {% set min_temp = printer.configfile.settings.extruder.min_extrude_temp|float %}
        {% set can_prime = (printer.extruder.temperature|float >= min_temp) %}
        {% if can_prime %}
            G91
            G1 E{e} F400
            G1 E-{r} F600
            G90
        {% endif %}

        CLEAN_NOZZLE
        M420 S1

        {% if printer.toolhead.homed_axes == "xyz" %}
            RESTORE_GCODE_STATE NAME=PAUSE MOVE=1 MOVE_SPEED=150
        {% else %}
            RESPOND TYPE=echo MSG="RESUME position restore skipped (axes not homed)"
        {% endif %}

        {% if can_prime %}
            G91
            G1 E{r} F600
            G90
        {% endif %}

        BASE_RESUME VELOCITY=150
    {% endif %}
